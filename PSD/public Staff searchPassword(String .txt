/**public Staff searchPassword(String pas) {
        for (Inventory each: list) {
            if (each instanceof Staff && pas.equals(.getPassword())) {
                return (Staff)each;
            }
        }
        return null;
    }**/